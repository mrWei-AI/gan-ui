import Row from './src/Row'
import Col from './src/Col'
import { withInstall } from '../../utils/with-install'
export const GRow=withInstall(Row)
export const GCol=withInstall(Col)
export default {
    GRow,
    GCol
}
